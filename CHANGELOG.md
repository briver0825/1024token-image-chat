# Changelog

本项目的所有重要变更都会记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## 维护规则

- 每次发布新版本前，必须更新本文件。
- 开发中的变更先记录到 `Unreleased`。
- 发布时，将 `Unreleased` 中的内容移动到新的版本标题下，并补充发布日期。
- 版本标题格式：`## [x.y.z] - YYYY-MM-DD`。
- 分类优先使用：`Added`、`Changed`、`Fixed`、`Removed`、`Security`。

## [Unreleased]

### Added

- 新增公共「焚决市场」，支持把已生成图片提交为公开作品。
- 新增 `/market` 独立页面，支持浏览市场作品、复制效果并带回聊天区生成。
- 新增市场作品提示词详情弹窗，支持查看完整提示词、生成参数并复制。
- 新增 SQLite + 本地文件的市场持久化存储，以及管理员口令软删除能力。

### Changed

- 调整焚决市场管理员删除入口：移除卡片内“管理删除”，改为顶部删除模式并支持勾选删除。
- 调整焚决市场“去生成”行为：带入提示词后进入新会话，避免追加到当前会话。

### Fixed

- 修复图片生成失败时只显示通用不可用文案的问题，现在会保留上游或网络错误的具体原因。
- 修复同一毫秒内创建用户提示词和助手图片时，聊天消息可能倒序显示的问题。
- 修复带参考图提交后输入区仍保留参考图，导致下一次生成意外继续引用的问题。

## [0.1.0] - 2026-04-26

### Added

- 支持最多 16 张参考图，按 OpenAI Images edits 的多图参考能力提交。
- 支持上传本地 PNG、JPEG、WebP 图片作为参考图。
- 支持从已生成图片继续添加到参考图列表。
- 支持在会话列表中通过右键菜单进行置顶和删除操作。

### Changed

- 优化参考图预览、移除和清空交互。
- 优化移动端输入栏底部布局，避免左下角拥挤和遮挡。
- 优化 PC 端输入栏底部布局，保持操作按钮横向排列。
- 将 Next.js 开发模式指示器移动到右上角，减少移动端遮挡。

### Fixed

- 修复侧边栏会话长标题导致内容显示不完整的问题。
- 修复历史消息中参考图资源加载不完整的问题。
- 修复刷新或切换到已有会话后聊天区不会自动滚动到底部的问题。
